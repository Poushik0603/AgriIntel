apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-market
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-market
  template:
    metadata:
      labels:
        app: mysql-market
    spec:
      containers:
        - name: mysql-market
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
            - name: MYSQL_DATABASE
              value: market_db
          ports:
            - containerPort: 3306
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-market
spec:
  selector:
    app: mysql-market
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP
